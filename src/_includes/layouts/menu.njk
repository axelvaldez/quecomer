<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ restaurant.name }}</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="page-menu"{% if restaurant.accentColor %} style="--accent-base: {{ restaurant.accentColor }}; --accent-darkest: color-mix(in srgb, {{ restaurant.accentColor }} 60%, #000); --accent-dark: color-mix(in srgb, {{ restaurant.accentColor }} 80%, #000); --accent-light: color-mix(in srgb, {{ restaurant.accentColor }} 60%, #fff); --accent-lightest: color-mix(in srgb, {{ restaurant.accentColor }} 20%, #fff); --color-bg-tinted: color-mix(in srgb, #f4f4f4 95%, {{ restaurant.accentColor }} 5%); --color-bg-dark-tinted: color-mix(in srgb, #f9f9f9 95%, {{ restaurant.accentColor }} 5%); --color-text-tinted: color-mix(in srgb, #1a1a1a 98%, {{ restaurant.accentColor }} 2%); --color-border-tinted: color-mix(in srgb, #eee 90%, {{ restaurant.accentColor }} 10%);"{% endif %}>
<div class="container">
    <nav>
        {% if restaurant.categories and restaurant.categories | length > 0 %}
            {% for cat in restaurant.categories %}
                <a href="/category/{{ cat | slugify }}/" class="category-pill">{{ cat }}</a>
            {% endfor %}
        {% else %}
            <a href="/" class="category-pill">Categorías</a>
        {% endif %}

        <a href="/" class="category-pill primary">All menus</a>
    </nav>
    <header>
        <h1>{{ restaurant.name }}</h1>
        <p class="tagline">{{ restaurant.tagline }}</p>
        <div class="meta-grid">
            <span>🕒 {{ restaurant.meta.hours }}</span> | <span>📷 {{ restaurant.meta.social }}</span>
        </div>
    </header>
    <main>
        {% for section in restaurant.menu %}
        <section>
            <h2 class="cat-title">{{ section.title }}</h2>
            {% if section.note %}<div class="note"><strong>Nota:</strong> {{ section.note }}</div>{% endif %}
            {% for group in section.groups %}
                {% if group.subtitle %}<h3 class="sub-title">{{ group.subtitle }}</h3>{% endif %}
                {% for item in group.items %}
                <article class="dish">
                    <div class="d-head"><span>{{ item.name }}</span><span>${{ item.price }}</span></div>
                    {% if item.description %}<p class="d-desc">{{ item.description }}</p>{% endif %}
                    {% if item.includes %}<small class="d-meta">{{ item.includes }}</small>{% endif %}
                    {% if item.variants %}
                    <div>{% for v in item.variants %}<span class="addon">{{ v.label }} {% if v.price %}{{ v.price }}{% endif %}</span>{% endfor %}</div>
                    {% endif %}
                </article>
                {% endfor %}
            {% endfor %}
        </section>
        {% endfor %}
    </main>
</div>
</body>
</html>
